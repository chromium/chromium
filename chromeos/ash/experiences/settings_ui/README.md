This directory hosts ChromeOS Settings System Web App implementation.

The name is "settings_ui" rather than just "settings" to avoid confusion
with `chromeos/ash/components/settings`, which hosts CrosSettings.
