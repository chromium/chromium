declare_args() {
  # Whether to enable the "real" ChromeOS Projector App. When false, a mock app is
  # bundled for testing integration points.
  # TODO(b/195650526): Set to is_chromeos_ash && is_chrome_branded once CIPD bundle available.
  enable_cros_projector_app = false
}
