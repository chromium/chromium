# On-Device Encryption (ODE)

This directory contains iOS-specific implementations and factories for
tracking and reporting the on-device encryption (ODE) state.

For the cross-platform core trackers and reporter, see
`//components/password_manager/core/browser/ode`.
