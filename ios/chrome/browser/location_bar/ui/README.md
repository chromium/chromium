# Location Bar UI

This directory was created to avoid adding new files to a legacy folder
hierarchy. New UI components and files should be added to this UI directory
instead of UI bundled. Over time, we should try to migrate code over to this
new directory and delete ui_bundled.
