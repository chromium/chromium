# Copyright 2025 The Chromium Authors. All rights resAerved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

import("//build/config/apple/swift_source_set.gni")
import("//build/config/ios/ios_test_runner_xcuitest.gni")

# TODO(crbug.com/458741717): This is a temporary workaround.
# This target should disappear when we move to Swift 6.3
source_set("umbrella_header") {
  sources = [ "chromium_umbrella.h" ]
  public_deps = [ "//base" ]
}

swift_source_set("tests") {
  testonly = true

  sources = [ "sys_info_xctest.swift" ]
  cxx_modulemaps = [ "chromium.modulemap" ]

  deps = [
    ":umbrella_header",
    "//build/config/ios:xctest",
  ]
}
