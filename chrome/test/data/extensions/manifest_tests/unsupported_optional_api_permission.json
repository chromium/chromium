{
  "name": "Optional permissions which cannot be optional",
  "version": "0.0.1.33",
  "manifest_version": 3,
  "description": "Extension with an optional permission listed which can't be optional (geolocation).",
  "optional_permissions": [
    "tabs",
    "geolocation"
  ]
}
