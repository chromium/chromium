The core interfaces (`BrowserDelegate` and `BrowserController`) are documented
and located in
[//chromeos/ash/components/browser_delegate/](../../../../chromeos/ash/components/browser_delegate/README.md).

This directory hosts the concrete implementations (`BrowserDelegateImpl`,
`BrowserControllerImpl`).
