# Copyright 2025 The Chromium Authors
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

assert(!is_android)

source_set("device_notifications") {
  sources = []
  public_deps = []

  if (!is_android) {
    sources += [
      "device_connection_tracker.h",
      "device_system_tray_icon.h",
    ]
    public_deps += [
      "//base",
      "//chrome/browser/profiles:profile",
      "//components/keyed_service/core",
      "//ui/gfx",
      "//url",
    ]
  }
}

source_set("unit_tests") {
  testonly = true
  sources = []
  deps = []
  public_deps = []

  if (!is_android) {
    sources += [
      "device_connection_tracker_unittest.h",
      "device_system_tray_icon_unittest.h",
      "device_test_utils.h",
    ]
    public_deps += [
      ":device_notifications",
      "//base",
      "//build:chromeos_buildflags",
      "//chrome/browser",
      "//chrome/browser/extensions",
      "//chrome/browser/extensions:test_support",
      "//chrome/test:test_support",
      "//extensions:test_support",
      "//extensions/common",
      "//testing/gmock",
      "//url",
    ]
  }

  if (!is_android && !is_chromeos) {
    sources += [ "device_status_icon_unittest.h" ]
    public_deps += [ "//chrome/browser/status_icons" ]
  }

  if (is_chromeos) {
    sources += [ "device_pinned_notification_unittest.h" ]
  }
}
