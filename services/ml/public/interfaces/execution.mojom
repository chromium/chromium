// Copyright 2017 The Chromium Authors. All rights reserved.
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

module ml.mojom;

interface Execution {
  setInput(uint32 index, handle<shared_buffer> buffer, uint32 length)
    => (int32 resultCode);
  setOutput(uint32 index, handle<shared_buffer> buffer, uint32 length)
    => (int32 resultCode);
  startCompute()
    => (int32 resultCode);
};