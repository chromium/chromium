# Routing Configuration

Maps feedback context to the appropriate training persona.

By default, all training tasks are routed to:

- **Analyzer:** `personas/core/analyzer.json`
- **Upgrader:** `personas/core/upgrader.json`
