{
  "name": "blink-spec",
  "version": "1.0.0",
  "mcpServers": {
    "blink-spec": {
      "command": "vpython3",
      "args": ["${workspacePath}${/}third_party${/}blink${/}tools${/}mcp_servers${/}blink_spec${/}server.py"],
      "env": {
        "BLINK_SPEC_GITHUB_API_KEY": "${BLINK_SPEC_GITHUB_API_KEY}"
      }
    }
  }
}
