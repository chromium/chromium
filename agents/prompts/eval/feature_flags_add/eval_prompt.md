# Evaluation of feature_flags_add prompt

### Context
The goal of this prompt is strictly follow the instructions provided in agents/prompts/eval/feature_flags_add/prompt.md but passes in a test flag named "FeatureFlagAddTest"

The resulting generated output will then be used in promptfoo based evaluation. This task should be called exactly once so that no duplicates are generated.