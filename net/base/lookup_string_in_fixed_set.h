// Copyright 2015 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

#ifndef NET_BASE_LOOKUP_STRING_IN_FIXED_SET_H_
#define NET_BASE_LOOKUP_STRING_IN_FIXED_SET_H_

#include <stdint.h>

#include <optional>
#include <string>
#include <string_view>
#include <vector>

#include "base/containers/span.h"
#include "net/base/net_export.h"
#include "net/base/registry_controlled_domain_constants.h"

namespace net {

// Looks up the string `key` with in a fixed set of strings. The set of strings
// must be known at compile time. It is converted to a graph structure named a
// DAFSA (Deterministic Acyclic Finite State Automaton) by the script
// make_dafsa.py during compilation. This permits efficient (in time and space)
// lookup. The graph generated by make_dafsa.py takes the form of a constant
// byte array which should be supplied via the |graph| parameter.  The return
// value is `std::nullopt`, or the int code associated with the input key in the
// DAFSA.
//
// TODO(nick): Replace this with FixedSetIncrementalLookup everywhere.
NET_EXPORT std::optional<int> LookupStringInFixedSet(
    base::span<const uint8_t> graph,
    std::string_view key);

struct SuffixMatch {
  DomainRuleTags tags;
  std::string_view suffix;
};

// Looks up the longest matching suffix for `host` in a reversed DAFSA. Partial
// matches must begin at a new component, i.e. `host` itself could match or a
// host part starting after a dot could match.
// If no match was found, returns `std::nullopt`. Otherwise, returns the tags
// and the matched suffix substring of `host`.
//
// This function must only be used on DAFSA graphs of registry-controlled
// domains which use the same encoding as //net/tools/tld_cleanup.cc.
std::optional<SuffixMatch> LookupSuffixInReversedSet(
    base::span<const uint8_t> graph,
    bool include_private,
    std::string_view host);

// Builds a vector of every string in the language of the DAFSA.
NET_EXPORT_PRIVATE std::vector<std::string> EnumerateDafsaLanguageForTesting(
    base::span<const uint8_t> graph);

}  // namespace net

#endif  // NET_BASE_LOOKUP_STRING_IN_FIXED_SET_H_
