// third_party/blink/public/mojom/locks/lock_manager.mojom.MojoProfile.swift is auto generated by mojom_bindings_generator.py, do not edit

// Copyright 2026 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

// This file is autogenerated by:
//     mojo/public/tools/bindings/generators/mojom_fuzzilli_generator.py
// For:
//     third_party/blink/public/mojom/locks/lock_manager.mojom
//

private let mojoPrefix = "blink.mojom."

///
/// Interfaces
///
extension ILType {
    // LockManager
    fileprivate static let jsBlinkMojomLockManagerRemote: ILType = .object(
        ofGroup: "blink.mojom.LockManagerRemote",
        withProperties: ["$"],
        withMethods: [
            "requestLock",
            "queryState",
        ])
    fileprivate static let jsBlinkMojomLockManagerRemoteWrapper: ILType = .object(
        ofGroup: "blink.mojom.LockManagerRemoteWrapper",
        withMethods: ["close", "isBound", "associateAndPassReceiver"])
    fileprivate static let jsBlinkMojomLockManagerPendingReceiver: ILType = .object(
        ofGroup: "blink.mojom.LockManagerPendingReceiver")

    // LockHandle
    fileprivate static let jsBlinkMojomLockHandleRemote: ILType = .object(
        ofGroup: "blink.mojom.LockHandleRemote",
        withProperties: ["$"],
        withMethods: [
        ])
    fileprivate static let jsBlinkMojomLockHandleRemoteWrapper: ILType = .object(
        ofGroup: "blink.mojom.LockHandleRemoteWrapper",
        withMethods: ["close", "isBound", "associateAndPassReceiver"])
    fileprivate static let jsBlinkMojomLockHandlePendingReceiver: ILType = .object(
        ofGroup: "blink.mojom.LockHandlePendingReceiver")

    // LockRequest
    fileprivate static let jsBlinkMojomLockRequestCallbackRouter: ILType = .object(
        ofGroup: "blink.mojom.LockRequestCallbackRouter",
        withProperties: [
            "$",
            "granted",
            "failed",
        ])
    fileprivate static let jsBlinkMojomLockRequestCallbackRouterReceiverHelper: ILType = .object(
        ofGroup: "blink.mojom.LockRequestCallbackRouterReceiverHelper",
        withMethods: ["associateAndPassRemote", "closeBindings"])
    fileprivate static let jsBlinkMojomLockRequestRemote: ILType = .object(
        ofGroup: "blink.mojom.LockRequestRemote",
        withProperties: ["$"])
    fileprivate static let jsBlinkMojomLockRequestRemoteWrapper: ILType = .object(
        ofGroup: "blink.mojom.LockRequestRemoteWrapper",
        withMethods: ["close", "isBound", "associateAndPassReceiver"])
    fileprivate static let jsBlinkMojomLockRequestPendingReceiver: ILType = .object(
        ofGroup: "blink.mojom.LockRequestPendingReceiver")
    fileprivate static let jsBlinkMojomLockRequestGrantedCallbackReceiver: ILType = .object(
        ofGroup: "BlinkMojomLockRequestGrantedCallbackReceiver",
        withMethods: ["addListener"])
    fileprivate static let jsBlinkMojomLockRequestFailedCallbackReceiver: ILType = .object(
        ofGroup: "BlinkMojomLockRequestFailedCallbackReceiver",
        withMethods: ["addListener"])
}

extension ObjectGroup {
    // LockManager
    fileprivate static let blinkMojomLockManagerRemote = ObjectGroup(
        name: "blink.mojom.LockManagerRemote",
        instanceType: .jsBlinkMojomLockManagerRemote,
        properties: ["$": .jsBlinkMojomLockManagerRemoteWrapper],
        methods: [
            "requestLock": [
                .plain(.string),
                .plain(.jsBlinkMojomLockMode),
                .plain(.jsBlinkMojomLockManager_WaitMode),
                .plain(.jsBlinkMojomLockRequestRemote),
            ] => .undefined,
            "queryState": [
            ] => .jsPromise(resolvingTo: .jsBlinkMojomLockManager_QueryState_ResponseParams),
        ]
    )

    fileprivate static let blinkMojomLockManagerRemoteWrapper = ObjectGroup(
        name: "blink.mojom.LockManagerRemoteWrapper",
        instanceType: .jsBlinkMojomLockManagerRemoteWrapper,
        properties: [:],
        methods: [
            "close": [] => .undefined,
            "isBound": [] => .boolean,
            "associateAndPassReceiver": [] => .jsBlinkMojomLockManagerPendingReceiver,
        ]
    )

    fileprivate static let blinkMojomLockManagerPendingReceiver = ObjectGroup(
        name: "blink.mojom.LockManagerPendingReceiver",
        instanceType: .jsBlinkMojomLockManagerPendingReceiver,
        properties: [:],
        methods: [:]
    )

    // LockHandle
    fileprivate static let blinkMojomLockHandleRemote = ObjectGroup(
        name: "blink.mojom.LockHandleRemote",
        instanceType: .jsBlinkMojomLockHandleRemote,
        properties: ["$": .jsBlinkMojomLockHandleRemoteWrapper],
        methods: [:]
    )

    fileprivate static let blinkMojomLockHandleRemoteWrapper = ObjectGroup(
        name: "blink.mojom.LockHandleRemoteWrapper",
        instanceType: .jsBlinkMojomLockHandleRemoteWrapper,
        properties: [:],
        methods: [
            "close": [] => .undefined,
            "isBound": [] => .boolean,
            "associateAndPassReceiver": [] => .jsBlinkMojomLockHandlePendingReceiver,
        ]
    )

    fileprivate static let blinkMojomLockHandlePendingReceiver = ObjectGroup(
        name: "blink.mojom.LockHandlePendingReceiver",
        instanceType: .jsBlinkMojomLockHandlePendingReceiver,
        properties: [:],
        methods: [:]
    )

    // LockRequest
    fileprivate static let blinkMojomLockRequestCallbackRouter = ObjectGroup(
        name: "blink.mojom.LockRequestCallbackRouter",
        instanceType: .jsBlinkMojomLockRequestCallbackRouter,
        properties: [
            "$": .jsBlinkMojomLockRequestCallbackRouterReceiverHelper,
            "granted": .jsBlinkMojomLockRequestGrantedCallbackReceiver,
            "failed": .jsBlinkMojomLockRequestFailedCallbackReceiver,
        ],
        methods: [:]
    )

    fileprivate static let blinkMojomLockRequestCallbackRouterReceiverHelper = ObjectGroup(
        name: "blink.mojom.LockRequestCallbackRouterReceiverHelper",
        instanceType: .jsBlinkMojomLockRequestCallbackRouterReceiverHelper,
        properties: [:],
        methods: [
            "associateAndPassRemote": [] => .jsBlinkMojomLockRequestRemote,
            "closeBindings": [] => .undefined,
        ]
    )

    fileprivate static let blinkMojomLockRequestGrantedCallbackReceiver = ObjectGroup(
        name: "BlinkMojomLockRequestGrantedCallbackReceiver",
        instanceType: .jsBlinkMojomLockRequestGrantedCallbackReceiver,
        properties: [:],
        methods: [
            "addListener": [
                .plain(
                    .function(
                        [
                            .plain(.jsBlinkMojomLockHandleRemote),
                        ] => .undefined))
            ] => .integer,
        ])

    fileprivate static let blinkMojomLockRequestFailedCallbackReceiver = ObjectGroup(
        name: "BlinkMojomLockRequestFailedCallbackReceiver",
        instanceType: .jsBlinkMojomLockRequestFailedCallbackReceiver,
        properties: [:],
        methods: [
            "addListener": [
                .plain(
                    .function(
                        [
                        ] => .undefined))
            ] => .integer,
        ])

    fileprivate static let blinkMojomLockRequestRemote = ObjectGroup(
        name: "blink.mojom.LockRequestRemote",
        instanceType: .jsBlinkMojomLockRequestRemote,
        properties: ["$": .jsBlinkMojomLockRequestRemoteWrapper],
        methods: [:]
    )

    fileprivate static let blinkMojomLockRequestRemoteWrapper = ObjectGroup(
        name: "blink.mojom.LockRequestRemoteWrapper",
        instanceType: .jsBlinkMojomLockRequestRemoteWrapper,
        properties: [:],
        methods: [
            "close": [] => .undefined,
            "isBound": [] => .boolean,
            "associateAndPassReceiver": [] => .jsBlinkMojomLockRequestPendingReceiver,
        ]
    )

    fileprivate static let blinkMojomLockRequestPendingReceiver = ObjectGroup(
        name: "blink.mojom.LockRequestPendingReceiver",
        instanceType: .jsBlinkMojomLockRequestPendingReceiver,
        properties: [:],
        methods: [:]
    )
}

///
/// Structs
///
extension ILType {
    fileprivate static let jsBlinkMojomLockInfo: ILType = .object(
        ofGroup: "blink.mojom.LockInfo",
        withProperties: [
            "name",
            "mode",
            "clientId",
        ])
    fileprivate static let jsBlinkMojomLockInfoConstructor: ILType = .constructor([
        .plain(.string),
        .plain(.jsBlinkMojomLockMode),
        .plain(.string),
    ] => .jsBlinkMojomLockInfo)
    fileprivate static let jsBlinkMojomLockManager_QueryState_ResponseParams: ILType = .object(
        ofGroup: "blink.mojom.LockManager_QueryState_ResponseParams",
        withProperties: [
            "requested",
            "held",
        ])
}

extension ObjectGroup {
    fileprivate static let blinkMojomLockInfo = ObjectGroup(
        name: "blink.mojom.LockInfo",
        instanceType: .jsBlinkMojomLockInfo,
        properties: [
            "name": .string,
            "mode": .jsBlinkMojomLockMode,
            "clientId": .string,
        ],
        methods: [:]
    )
    fileprivate static let blinkMojomLockManagerQueryStateResponseParams = ObjectGroup(
        name: "blink.mojom.LockManager_QueryState_ResponseParams",
        instanceType: .jsBlinkMojomLockManager_QueryState_ResponseParams,
        properties: [
            "requested": .jsBlinkMojomLockInfoArray,
            "held": .jsBlinkMojomLockInfoArray,
        ],
        methods: [:]
    )
}

///
/// Arrays
///
extension ILType {
    fileprivate static let jsBlinkMojomLockInfoArray: ILType =
        .createJsArrayType(ofElementType: .jsBlinkMojomLockInfo)
}

/// As Fuzzilli runs, the OperationMutator and VariadicInputReducer will modify
/// the `CreateArray` operation to increase or decrease the element count. This
/// allows arrays to be arbitrarily long.
private let MojoBlinkMojomLockInfoArrayGenerator = CodeGenerator(
    "MojoBlinkMojomLockInfoArrayGenerator",
    inputs: .required(.jsBlinkMojomLockInfo),
    produces: [.jsBlinkMojomLockInfoArray]
) { b, elem in
    var elements: [Variable] = [elem]
    b.createArray(
        with: elements,
        elementGroupName:
            ObjectGroup.blinkMojomLockInfo.name)
}

///
/// Enumerations
///
extension ILType {
    fileprivate static let jsBlinkMojomLockMode: ILType = .intEnumeration(
        ofName: "blink.mojom.LockMode",
        withValues: [0, 1])
    fileprivate static let jsBlinkMojomLockManager_WaitMode: ILType = .intEnumeration(
        ofName: "blink.mojom.LockManager_WaitMode",
        withValues: [0, 1, 2])
}

///
/// Generators
///
// TODO(crbug.com/500386713) Consider using mojoBuiltins or another list instead of regex
private func isTargetObject(type: ILType) -> Bool {
    guard type.Is(.object()), let group = type.group else { return false }
    return group.starts(with: mojoPrefix)
}

/// Mojo variant of the builtin `MethodCallGenerator` that operates only on
/// "Mojo" objects instead of all the objects in the JavaScript environment,
/// most of which are unrelated to the interface. This generator emits random
/// method calls, generating any arguments that are not available.
private let MojoMethodCallGenerator = CodeGenerator("MojoMethodCallGenerator") {
    b in
    // Pick a variable among all Mojo variables with methods
    var targetVar = b.findVariable {
        let type = b.type(of: $0)
        return isTargetObject(type: type) && !type.methods.isEmpty
    }

    // If we can't find a Mojo object in scope, create the LockManager remote
    guard let obj = targetVar else {
        let getRemoteFunc = b.createNamedVariable(forBuiltin: "getOrCreatePrimaryRemote")
        b.callFunction(getRemoteFunc, withArgs: [])
        return
    }

    let methodName = b.type(of: obj).randomMethod()!
    let signatures = b.methodSignatures(of: methodName, on: obj)
    let signature = chooseUniform(from: signatures)
    let arguments = b.findOrGenerateArguments(forSignature: signature)
    b.callMethod(methodName, on: obj, withArgs: arguments, guard: false)
}

/// Mojo variant of the builtin `PropertyRetrievalGenerator` that operates only
/// on "Mojo" objects instead of all the objects in the JavaScript environment,
/// most of which are unrelated to the interface. This generator emits random
/// property retrievals.
private let MojoPropertyRetrievalGenerator = CodeGenerator(
    "MojoPropertyRetrievalGenerator"
) { b in
    let targetVar = b.findVariable { isTargetObject(type: b.type(of: $0)) }
    guard let obj = targetVar else { return }

    let propertyName =
        b.type(of: obj).randomProperty() ?? b.randomCustomPropertyName()
    b.getProperty(propertyName, of: obj)
}

/// Generates a call to add a callback to the blinkMojomLockRequestCallbackRouter.
///
/// The callback has one parameter, specified with `.parameters(n: 1)`. `build(n: 5)`
/// then tells Fuzzilli to populate the callback body with five random expressions.
private let MojoBlinkMojomLockRequestRouterListenerGenerator = CodeGenerator(
    "MojoBlinkMojomLockRequestRouterListenerGenerator",
    inputs: .required(.jsBlinkMojomLockRequestCallbackRouter)
) { b, router in
    guard
        let eventName = ILType.jsBlinkMojomLockRequestCallbackRouter.properties
            .subtracting(["$"])
            .randomElement()
    else {
        return
    }

    let listenerHost = b.getProperty(eventName, of: router)
    let callback = b.buildPlainFunction(with: .parameters(n: 1)) { args in
        if probability(0.2) {
            b.buildIf(args[0]) {
                let wrapper = b.getProperty("$", of: args[0])
                b.callMethod("close", on: wrapper, withArgs: [])
            }
        }
        b.build(n: 5)
    }
    b.callMethod("addListener", on: listenerHost, withArgs: [callback])
}

private let mojoBuiltins: [String: ILType] = [
    /// Profiles declare this method, a wrapper method around `getRemote` that follows
    /// the Singleton pattern, in `codePrefix`. This method ensures: (1) `getRemote`
    /// is only called once, and (2) the remote is saved to a global variable so it
    /// can be closed at the end of the program.
    "getOrCreatePrimaryRemote": .function([] => .jsBlinkMojomLockManagerRemote),
    "blink.mojom.LockRequestCallbackRouter":
       .constructor([] => .jsBlinkMojomLockRequestCallbackRouter),
    "blink.mojom.LockInfo": .jsBlinkMojomLockInfoConstructor,
]

private let keepGenerators = [
    "IntegerGenerator", "StringGenerator", "TernaryOperationGenerator",
    "PlainFunctionGenerator", "AsyncFunctionGenerator", "AwaitGenerator",
    "SubroutineReturnGenerator", "TryCatchGenerator",
]

private let mojoDisabledGenerators: [String] = CodeGenerators.map { $0.name }
    .filter {
        !keepGenerators.contains($0)
    }

let mojoLockManagerProfile = Profile(
    processArgs: { _ in return [] },
    processArgsReference: nil,
    processEnv: [
        "ASAN_OPTIONS": "detect_odr_violation=0:abort_on_error=1", "DISPLAY": ":20",
    ],
    maxExecsBeforeRespawn: 1000,
    timeout: Timeout.interval(11000, 11000),
    codePrefix: """
        let globalRemote = null;
        function getOrCreatePrimaryRemote() {
            if (!globalRemote) {
                globalRemote = blink.mojom.LockManager.getRemote();
            }
            return globalRemote;
        }
        """,
    codeSuffix: "if (globalRemote) {globalRemote.$.close()}",
    ecmaVersion: v8Profile.ecmaVersion,
    startupTests: [
        ("fuzzilli('FUZZILLI_PRINT', 'test')", .shouldSucceed),
        (
            """
            if (typeof blink == 'undefined' \
            || blink?.mojom?.LockManager === undefined) \
            throw 'LockManager not found'
            """,
            .shouldSucceed
        ),
    ] + v8Profile.startupTests,
    additionalCodeGenerators: [
        (MojoMethodCallGenerator, 10000),
        (MojoPropertyRetrievalGenerator, 10000),
        (MojoBlinkMojomLockRequestRouterListenerGenerator, 5000),
        (MojoBlinkMojomLockInfoArrayGenerator, 1),
    ] + commonMojoCodeGenerators,
    additionalProgramTemplates: WeightedList([]),
    disabledCodeGenerators: mojoDisabledGenerators,
    disabledMutators: [
        "ExplorationMutator", "ProbingMutator", "PropertyAccessorMutator"
    ],
    additionalBuiltins: mojoBuiltins.merging(commonMojoBuiltins) { (existing, _) in existing },
    additionalObjectGroups: [
        .blinkMojomLockManagerRemote,
        .blinkMojomLockManagerRemoteWrapper,
        .blinkMojomLockManagerPendingReceiver,
        .blinkMojomLockHandleRemote,
        .blinkMojomLockHandleRemoteWrapper,
        .blinkMojomLockHandlePendingReceiver,
        .blinkMojomLockRequestCallbackRouter,
        .blinkMojomLockRequestCallbackRouterReceiverHelper,
        .blinkMojomLockRequestRemote,
        .blinkMojomLockRequestRemoteWrapper,
        .blinkMojomLockRequestPendingReceiver,
        .blinkMojomLockRequestGrantedCallbackReceiver,
        .blinkMojomLockRequestFailedCallbackReceiver,
        .blinkMojomLockInfo,
        .blinkMojomLockManagerQueryStateResponseParams,
    ] + commonMojoObjectGroups,
    additionalEnumerations: [
        .jsBlinkMojomLockMode,
        .jsBlinkMojomLockManager_WaitMode,
    ] + commonMojoEnumerations,
    additionalOptionsBags: [] + commonMojoOptionsBags,
    optionalPostProcessor: nil
)
