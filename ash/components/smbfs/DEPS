include_rules = [
  "+chromeos/components/mojo_bootstrap",

  # TODO(https://crbug.com/1164001) : Remove when chromeos/disks moved to ash.
  "+chromeos/disks",
]

specific_include_rules = {
  "smbfs_mounter_unittest\.cc": [
    "+mojo/core/embedder",
  ],
}
