{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "PersonaDef": {
      "type": "object",
      "properties": {
        "role": { "type": "string" },
        "mandate": { "oneOf": [{ "type": "string" }, { "type": "array", "items": { "type": "string" } }] },
        "checklist": {
          "type": "object",
          "patternProperties": {
            "^.*$": { "oneOf": [{ "type": "string" }, { "type": "array", "items": { "type": "string" } }] }
          }
        }
      },
      "required": ["role", "mandate", "checklist"]
    },
    "ChecklistObject": {
      "type": "object",
      "description": "A map of boolean flags representing checklist items.",
      "patternProperties": {
        "^.*$": { "type": "boolean" }
      }
    },
    "EnvironmentDef": {
      "type": "object",
      "properties": {
        "vcs": { "type": "string", "enum": ["GIT", "JJ"] },
        "harness": { "type": "string", "enum": ["JETSKI", "GENERIC_CLI"] },
        "repo_type": {
          "type": "string",
          "enum": ["CHROMIUM", "GOOGLE_INTERNAL"]
        },
        "output_directory": { "type": "string" }
      },
      "required": ["vcs", "harness", "repo_type"]
    },
    "StateBlock": {
      "type": "object",
      "properties": {
        "checklist": { "$ref": "#/definitions/ChecklistObject" },
        "unlisted_issues_found": {
          "type": "array",
          "items": { "type": "string" }
        },
        "iteration": { "type": "integer" },
        "oscillation_detected": { "type": "boolean" },
        "conflict_report": {
          "type": "array",
          "items": { "type": "string" }
        },
        "active_constraints": {
          "type": "array",
          "items": { "type": "string" }
        },
        "resolved_constraints": {
          "type": "array",
          "items": { "type": "string" }
        },
        "ignored_constraints": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "constraint": { "type": "string" },
              "rationale": { "type": "string" }
            },
            "required": ["constraint", "rationale"]
          }
        },
        "next_stage": {
          "type": "string",
          "enum": [
            "CRITIQUE", "SCAFFOLDING", "PREPARATION", "IMPLEMENTATION",
            "SYNTHESIS", "TEST_FILLING", "ANALYSIS",
            "TRAINING", "ESCALATION", "VALIDATION", "DEPLOYMENT"
          ]
        },
        "state_transport": {
          "type": "string",
          "enum": ["FILE_IO", "EPHEMERAL", "EPHEMERAL_WITH_LOGS"]
        }
      },
      "required": [
        "checklist",
        "iteration",
        "oscillation_detected",
        "active_constraints",
        "resolved_constraints",
        "state_transport",
        "next_stage"
      ]
    },
    "ProjectSpec": {
      "type": "object",
      "properties": {
        "task_type": {
          "type": "string",
          "enum": ["IMPLEMENTATION", "REVIEW", "AUDIT"]
        },
        "execution_path": {
          "type": "string",
          "enum": ["FAST_PATH", "RIGOR_PATH"]
        },
        "complexity_level": {
          "type": "string",
          "enum": ["LOW", "MEDIUM", "HIGH"]
        },
        "unlisted_issues_found": {
          "type": "array",
          "items": { "type": "string" }
        },
        "goal": { "type": "string" },
        "target_files": { "type": "array", "items": { "type": "string" } },
        "anti_goals": { "type": "array", "items": { "type": "string" } },
        "edge_cases": { "type": "array", "items": { "type": "string" } },
        "build_targets": { "type": "array", "items": { "type": "string" } },
        "next_stage": {
          "type": "string",
          "enum": ["SCAFFOLDING", "PREPARATION"]
        },
        "paranoia_mode": { "type": "boolean" },
        "auditability_level": {
          "type": "string",
          "enum": ["NORMAL", "VERBOSE"]
        },
        "context_resolved": {
          "type": "boolean",
          "description": "True only if all external bugs/links were successfully read and no hallucinations occurred."
        },
        "approach_confirmed": {
          "type": "boolean",
          "description": "True only if the human has explicitly approved the goal and target files."
        },
        "ambiguity_level": {
          "type": "string",
          "enum": ["LOW", "HIGH"],
          "description": "Assessment of how prescriptive the human instructions were versus how much discovery/decision-making is required."
        },
        "ambiguity_rationale": {
          "type": "string",
          "description": "Brief explanation justifying the ambiguity_level."
        },
        "environment": { "$ref": "#/definitions/EnvironmentDef" }
      },
      "required": [
        "task_type",
        "execution_path",
        "complexity_level",
        "goal",
        "target_files",
        "anti_goals",
        "edge_cases",
        "next_stage",
        "paranoia_mode",
        "auditability_level",
        "context_resolved",
        "approach_confirmed",
        "ambiguity_level",
        "ambiguity_rationale",
        "environment"
      ]
    },
    "ReviewFeedback": {
      "type": "object",
      "properties": {
        "checklist": { "$ref": "#/definitions/ChecklistObject" },
        "unlisted_issues_found": {
          "type": "array",
          "items": { "type": "string" }
        },
        "verdict": { "type": "string", "enum": ["ACCEPT", "REJECT"] },
        "reasoning": { "type": "array", "items": { "type": "string" } },
        "comments": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "file": { "type": "string" },
              "line": { "type": "integer" },
              "comment": { "type": "string" }
            },
            "required": ["file", "comment"]
          }
        },
        "next_stage": {
          "type": "string",
          "enum": ["ANALYSIS"]
        }
      },
      "required": ["checklist", "verdict", "reasoning", "next_stage"]
    },
    "Constraints": {
      "type": "object",
      "properties": {
        "checklist": { "$ref": "#/definitions/ChecklistObject" },
        "unlisted_issues_found": {
          "type": "array",
          "items": { "type": "string" }
        },
        "iteration": { "type": "integer" },
        "oscillation_detected": { "type": "boolean" },
        "conflict_report": {
          "type": "array",
          "items": { "type": "string" }
        },
        "constraints": { "type": "array", "items": { "type": "string" } },
        "next_stage": {
          "type": "string",
          "enum": ["SYNTHESIS", "TRAINING", "ESCALATION"]
        }
      },
      "required": [
        "checklist",
        "iteration",
        "oscillation_detected",
        "constraints",
        "next_stage"
      ]
    }
  }
}
