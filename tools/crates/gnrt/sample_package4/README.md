This is a sample Cargo package to test `gnrt`'s ability to detect and complain
about scenarios where multiple epochs/version of the same crate are present
in the dependency tree.

`tools/crates/gnrt/lib/test_metadata4.json` is generated by the output of
running `cargo metadata --format-version 1` in this directory.
