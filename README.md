# dappnet-chromium

This is a fork of Chromium.

Adds support for .eth and .dapp domains, which are resolved to content on P2P networks, via a blockchain for domain resolution (decentralized DNS).

## Status.

Working PoC.