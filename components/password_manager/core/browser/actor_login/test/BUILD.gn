# Copyright 2025 The Chromium Authors
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

import("//build/config/features.gni")

assert(!is_android)

source_set("test_support") {
  testonly = true

  sources = [
    "mock_actor_login_delegate.cc",
    "mock_actor_login_delegate.h",
  ]

  visibility = [ "//chrome/browser/password_manager/actor_login:*" ]

  deps = [
    "//base",
    "//components/password_manager/core/browser/actor_login:common",
    "//components/password_manager/core/browser/actor_login/internal:delegate",
    "//testing/gmock",
  ]
}
