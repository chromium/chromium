include_rules = [
  "+components/browser_ui/bottomsheet/android",
  "+components/browser_ui/widget",  # Needed for integration tests (unit_device_javatests).
  "+components/payments/content/android/ui",
  "+ui/android",  # Needed for integration tests (unit_device_javatests).
]
