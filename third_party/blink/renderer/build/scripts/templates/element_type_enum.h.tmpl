{% from "templates/macros.tmpl" import license, source_files_for_generated_file %}
{{ license() }}

{{source_files_for_generated_file(template_file, input_files)}}

#ifndef THIRD_PARTY_BLINK_RENDERER_CORE_HTML_ELEMENT_TYPE_ENUM_H_
#define THIRD_PARTY_BLINK_RENDERER_CORE_HTML_ELEMENT_TYPE_ENUM_H_

#include <stdint.h>

namespace blink {

// NOTE: uint8_t is because Clang seemingly does not want to do
// the const vtable propagation optimization otherwise.
enum class ElementType : uint8_t {
  kIsNotElement = 0,
  {% for element in elements|sort %}k{{element}},
  {% endfor %}
};

}  // namespace blink

#endif  // THIRD_PARTY_BLINK_RENDERER_CORE_HTML_ELEMENT_TYPE_ENUM_H_
