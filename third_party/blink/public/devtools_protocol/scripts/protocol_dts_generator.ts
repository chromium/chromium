// Copyright 2026 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

/*
 * This script was taken from
 * https://github.com/ChromeDevTools/devtools-protocol/tree/master/scripts
 * and adjusted to read its input from the PDL-derived protocol JSON that is
 * produced by //third_party/blink/public/devtools_protocol:protocol_version
 * instead of from the checked in JSON of the `devtools-protocol` npm package.
 *
 * It emits the exact same set of files as the upstream generator, so that the
 * result is a drop-in replacement for the `devtools-protocol` npm package:
 *   - protocol.d.ts
 *   - protocol-mapping.d.ts
 *   - protocol-proxy-api.d.ts
 *   - protocol-tests-proxy-api.d.ts
 *
 * Usage:
 *   node protocol_dts_generator.ts --output-dir <dir> <protocol.json>...
 */

import * as fs from 'node:fs';
import * as path from 'node:path';

import type {IProtocol, Protocol as P} from './protocol_schema.d.ts';

const EventStyle = {
  REGULAR: 0,
  // See
  // https://crsrc.org/c/third_party/blink/web_tests/http/tests/inspector-protocol/resources/inspector-protocol-test.js
  INSPECTOR_PROTOCOL_TESTS: 1,
} as const;

type EventStyleEnum = (typeof EventStyle)[keyof typeof EventStyle];

let numIndents = 0;
let emitStr = '';

const emit = (str: string) => {
  emitStr += str;
};

const getIndent = () => '    '.repeat(numIndents);  // 4 spaced indents

const emitIndent = () => {
  emitStr += getIndent();
};

const emitLine = (str?: string) => {
  if (str) {
    emitIndent();
    emit(`${str}\n`);
  } else {
    emit('\n');
  }
};

const emitOpenBlock = (str: string, openChar = ' {') => {
  emitLine(`${str}${openChar}`);
  numIndents++;
};

const emitCloseBlock = (closeChar = '}') => {
  numIndents--;
  emitLine(closeChar);
};

const emitHeaderComments = () => {
  emitLine('// Copyright 2026 The Chromium Authors');
  emitLine(
      '// Use of this source code is governed by a BSD-style license that can be');
  emitLine('// found in the LICENSE file.');
  emitLine();
  emitLine(
      '/**********************************************************************');
  emitLine(
      ' * Auto-generated by protocol_dts_generator.ts. Do not edit manually. *');
  emitLine(
      ' **********************************************************************/');
  emitLine();
};

const fixCamelCase = (name: string): string => {
  let prefix = '';
  let result = name;
  if (name[0] === '-') {
    prefix = 'Negative';
    result = name.substring(1);
  }
  const refined = result.split('-').map(toTitleCase).join('');
  return prefix +
      refined.replace(/HTML|XML|WML|API/i, match => match.toUpperCase());
};

const emitEnum = (enumName: string, enumValues: string[]) => {
  emitOpenBlock(`export const enum ${enumName}`);
  enumValues.forEach(value => {
    emitLine(`${fixCamelCase(value)} = '${value}',`);
  });
  emitCloseBlock();
};

const emitPublicDocDeclaration = () => {
  emitLine('/**');
  emitLine(' * The Chrome DevTools Protocol.');
  emitLine(' * @public');
  emitLine(' */');
};

const emitModule = (moduleName: string, domains: P.Domain[]) => {
  moduleName = toTitleCase(moduleName);
  emitHeaderComments();
  emitPublicDocDeclaration();
  emitOpenBlock(`export namespace ${moduleName}`);
  emitGlobalTypeDefs();
  domains.forEach(emitDomain);
  emitCloseBlock();
  emitLine();
  emitLine(`export default ${moduleName};`);
};

const emitGlobalTypeDefs = () => {
  emitLine();
  emitLine(`export type integer = number`);
  emitLine(`export type binary = string`);
};

const emitDomain = (domain: P.Domain) => {
  const domainName = toTitleCase(domain.domain);
  emitLine();
  emitTsComment(domain);
  emitOpenBlock(`export namespace ${domainName}`);
  if (domain.types) {
    domain.types.forEach(emitDomainType);
  }
  if (domain.commands) {
    domain.commands.forEach(emitCommand);
  }
  if (domain.events) {
    domain.events.forEach(emitEvent);
  }
  emitCloseBlock();
};

const getCommentLines = (description: string) => {
  return description.split(/\r?\n/g).filter(line => !line.startsWith('LINT.'));
};

const emitDescription = (lines: string[]) => {
  emitLine(`/**`);
  lines.map(l => emitLine(` * ${l.replaceAll('*/', '*\\/')}`));
  emitLine(` */`);
};

const emitTsComment =
    (object: P.Event|P.PropertyType|P.DomainType|P.Command|P.Domain) => {
      const commentLines =
          object.description ? getCommentLines(object.description) : [];
      if ('deprecated' in object && object.deprecated) {
        commentLines.push('@deprecated');
      }

      if ('experimental' in object && object.experimental) {
        commentLines.push('@experimental');
      }

      if (commentLines.length) {
        emitDescription(commentLines);
      }
    };

const isPropertyInlineEnum = (prop: P.ProtocolType): boolean => {
  if ('$ref' in prop) {
    return false;
  }
  return prop.type === 'string' && prop.enum !== null &&
      prop.enum !== undefined;
};

const getPropertyDef =
    (interfaceName: string, prop: P.PropertyType): string => {
      // Quote key if it has a . in it.
      const propName = prop.name.includes('.') ? `'${prop.name}'` : prop.name;
      const type = getPropertyType(interfaceName, prop);
      return `${propName}${prop.optional ? '?' : ''}: ${type}`;
    };

const getPropertyType =
    (interfaceName: string, prop: P.ProtocolType): string => {
      if ('$ref' in prop) {
        return prop.$ref;
      } else if (prop.type === 'array') {
        return `${getPropertyType(interfaceName, prop.items)}[]`;
      } else if (prop.type === 'object') {
        if (!prop.properties) {
          // TODO: actually 'any'? or can use generic '[key: string]: string'?
          return `any`;
        } else {
          // hack: access indent, \n directly
          let objStr = '{\n';
          numIndents++;
          objStr += prop.properties
                        .map(
                            p => `${getIndent()}${
                                getPropertyDef(interfaceName, p)};\n`)
                        .join('');
          numIndents--;
          objStr += `${getIndent()}}`;
          return objStr;
        }
      } else if (prop.type === 'string' && prop.enum) {
        return '(' + prop.enum.map((v: string) => `'${v}'`).join(' | ') + ')';
      }
      return prop.type;
    };

const emitProperty = (interfaceName: string, prop: P.PropertyType) => {
  emitTsComment(prop);
  emitLine(`${getPropertyDef(interfaceName, prop)};`);
};

const emitInlineEnumForDomainType = (type: P.DomainType) => {
  if (type.type === 'object') {
    emitInlineEnums(type.id, type.properties);
  }
};

const emitInlineEnumsForCommands = (command: P.Command) => {
  emitInlineEnums(toCmdRequestName(command.name), command.parameters);
  emitInlineEnums(toCmdResponseName(command.name), command.returns);
};

const emitInlineEnumsForEvents = (event: P.Event) => {
  emitInlineEnums(toEventPayloadName(event.name), event.parameters);
};

const emitInlineEnums = (prefix: string, propertyTypes?: P.PropertyType[]) => {
  if (!propertyTypes) {
    return;
  }
  for (const type of propertyTypes) {
    if (isPropertyInlineEnum(type)) {
      emitLine();
      const enumName = prefix + toTitleCase(type.name);
      emitEnum(enumName, (type as P.StringType).enum || []);
    }
  }
};

const emitInterface = (interfaceName: string, props?: P.PropertyType[]) => {
  emitOpenBlock(`export interface ${interfaceName}`);
  props ? props.forEach(prop => emitProperty(interfaceName, prop)) :
          emitLine('[key: string]: string;');
  emitCloseBlock();
};

const emitDomainType = (type: P.DomainType) => {
  emitInlineEnumForDomainType(type);
  emitLine();
  emitTsComment(type);

  if (type.type === 'object') {
    emitInterface(type.id, type.properties);
  } else {
    emitLine(`export type ${type.id} = ${getPropertyType(type.id, type)};`);
  }
};

const toTitleCase = (str: string) => str[0].toUpperCase() + str.substring(1);

const toCmdRequestName = (commandName: string) =>
    `${toTitleCase(commandName)}Request`;

const toCmdResponseName = (commandName: string) =>
    `${toTitleCase(commandName)}Response`;

const emitCommand = (command: P.Command) => {
  emitInlineEnumsForCommands(command);
  // TODO(bckenny): should description be emitted for params and return types?
  if (command.parameters) {
    emitLine();
    emitInterface(toCmdRequestName(command.name), command.parameters);
  }

  if (command.returns) {
    emitLine();
    emitInterface(toCmdResponseName(command.name), command.returns);
  }
};

const toEventPayloadName = (eventName: string) =>
    `${toTitleCase(eventName)}Event`;

const emitEvent = (event: P.Event) => {
  if (!event.parameters) {
    return;
  }

  emitInlineEnumsForEvents(event);
  emitLine();
  emitTsComment(event);
  emitInterface(toEventPayloadName(event.name), event.parameters);
};

const getEventMapping =
    (event: P.Event, domainName: string, modulePrefix: string): P.RefType&
    P.PropertyBaseType&P.ExtraInformation => {
      // Use TS3.0+ tuples
      const payloadType = event.parameters ?
          `[${modulePrefix}.${domainName}.${toEventPayloadName(event.name)}]` :
          '[]';

      return {
        // domain-prefixed name since it will be used outside of the module.
        name: `${domainName}.${event.name}`,
        description: event.description,
        deprecated: event.deprecated,
        experimental: event.experimental,
        $ref: payloadType,
      };
    };

const isWeakInterface = (params: P.PropertyType[]): boolean => {
  return params.every(p => !!p.optional);
};

const getCommandMapping = (command: P.Command, domainName: string,
                           modulePrefix: string): P.ObjectType&
                          P.PropertyBaseType&P.ExtraInformation => {
  const prefix = `${modulePrefix}.${domainName}.`;
  // Use TS3.0+ tuples for paramsType
  let requestType = '[]';
  if (command.parameters) {
    const optional = isWeakInterface(command.parameters) ? '?' : '';
    requestType =
        '[' + prefix + toCmdRequestName(command.name) + optional + ']';
  }
  const responseType =
      command.returns ? prefix + toCmdResponseName(command.name) : 'void';

  return {
    type: 'object',
    name: `${domainName}.${command.name}`,
    description: command.description,
    deprecated: command.deprecated,
    experimental: command.experimental,
    properties: [
      {
        name: 'paramsType',
        $ref: requestType,
      },
      {
        name: 'returnType',
        $ref: responseType,
      },
    ],
  };
};

const flatten = <T>(arr: T[][]) => ([] as T[]).concat(...arr);

const emitMapping =
    (moduleName: string, protocolModuleName: string, domains: P.Domain[]) => {
      moduleName = toTitleCase(moduleName);
      emitHeaderComments();
      emitLine(`import Protocol from './${protocolModuleName}.js'`);
      emitLine();
      emitDescription([
        'Mappings from protocol event and command names to the types required for them.',
      ]);
      emitOpenBlock(`export namespace ${moduleName}`);

      const protocolModulePrefix = toTitleCase(protocolModuleName);
      const eventDefs = flatten(domains.map(d => {
        const domainName = toTitleCase(d.domain);
        return (d.events || [])
            .map(e => getEventMapping(e, domainName, protocolModulePrefix));
      }));
      emitInterface('Events', eventDefs);

      emitLine();

      const commandDefs = flatten(domains.map(d => {
        const domainName = toTitleCase(d.domain);
        return (d.commands || [])
            .map(c => getCommandMapping(c, domainName, protocolModulePrefix));
      }));
      emitInterface('Commands', commandDefs);

      emitCloseBlock();
      emitLine();
      emitLine(`export default ${moduleName};`);
    };

const emitApiCommand =
    (command: P.Command, domainName: string, modulePrefix: string,
     eventStyle: EventStyleEnum) => {
      const prefix = `${modulePrefix}.${domainName}.`;
      emitTsComment(command);
      const params = command.parameters ?
          `params: ${prefix}${toCmdRequestName(command.name)}` :
          '';
      const response = command.returns ?
          `${prefix}${toCmdResponseName(command.name)}` :
          'void';
      switch (eventStyle) {
        case EventStyle.REGULAR: {
          emitLine(`${command.name}(${params}): Promise<${response}>;`);
          break;
        }
        case EventStyle.INSPECTOR_PROTOCOL_TESTS: {
          emitLine(`${command.name}(${params}): Promise<{id: number, result: ${
              response}, sessionId: string}>;`);
          break;
        }
      }
      emitLine();
    };

const emitApiEvent =
    (event: P.Event, domainName: string, modulePrefix: string,
     eventStyle: EventStyleEnum) => {
      const prefix = `${modulePrefix}.${domainName}.`;
      emitTsComment(event);
      switch (eventStyle) {
        case EventStyle.REGULAR: {
          const params = event.parameters ?
              `params: ${prefix}${toEventPayloadName(event.name)}` :
              '';
          emitLine(`on(event: '${event.name}', listener: (${
              params}) => void): void;`);
          break;
        }
        case EventStyle.INSPECTOR_PROTOCOL_TESTS: {
          const returnType = event.parameters ?
              `{ params: ${prefix}${toEventPayloadName(event.name)} }` :
              'void';
          const eventType = event.parameters ? `event: ${returnType}` : '';
          emitLine(`on${toTitleCase(event.name)}(listener: (${
              eventType}) => void): void;`);
          emitLine(`off${toTitleCase(event.name)}(listener: (${
              eventType}) => void): void;`);
          emitLine(`once${toTitleCase(event.name)}(eventMatcher?: (${
              eventType}) => boolean): Promise<${returnType}>;`);
          break;
        }
      }
      emitLine();
    };

const emitDomainApi =
    (domain: P.Domain, modulePrefix: string, eventStyle: EventStyleEnum) => {
      emitLine();
      const domainName = toTitleCase(domain.domain);
      emitOpenBlock(`export interface ${domainName}Api`);
      if (domain.commands) {
        domain.commands.forEach(
            c => emitApiCommand(c, domainName, modulePrefix, eventStyle));
      }
      if (domain.events) {
        domain.events.forEach(
            e => emitApiEvent(e, domainName, modulePrefix, eventStyle));
      }
      emitCloseBlock();
    };

const emitApi =
    (moduleName: string, protocolModuleName: string, domains: P.Domain[],
     eventStyle: EventStyleEnum = EventStyle.REGULAR) => {
      moduleName = toTitleCase(moduleName);
      emitHeaderComments();
      emitLine(`import Protocol from './${protocolModuleName}.js'`);
      emitLine();
      emitDescription(['API generated from Protocol commands and events.']);
      emitOpenBlock(`export namespace ${moduleName}`);

      emitLine();
      emitOpenBlock(`export interface ProtocolApi`);
      domains.forEach(d => {
        emitLine(`${d.domain}: ${d.domain}Api;`);
        emitLine();
      });
      emitCloseBlock();
      emitLine();

      const protocolModulePrefix = toTitleCase(protocolModuleName);
      domains.forEach(d => emitDomainApi(d, protocolModulePrefix, eventStyle));
      emitCloseBlock();

      emitLine();
      emitLine(`export default ${moduleName};`);
    };

const flushEmitToFile = (filePath: string) => {
  fs.mkdirSync(path.dirname(filePath), {recursive: true});
  fs.writeFileSync(filePath, emitStr, {encoding: 'utf-8'});

  numIndents = 0;
  emitStr = '';
};

const parseArgs = (argv: string[]) => {
  let outputDir: string|undefined;
  const protocolFiles: string[] = [];
  for (let i = 0; i < argv.length; i++) {
    const arg = argv[i];
    if (arg === '--output-dir') {
      outputDir = argv[++i];
    } else if (arg.startsWith('--')) {
      throw new Error(`Unknown argument: ${arg}`);
    } else {
      protocolFiles.push(arg);
    }
  }
  if (!outputDir) {
    throw new Error('--output-dir is required');
  }
  if (protocolFiles.length === 0) {
    throw new Error('At least one protocol JSON file is required');
  }
  return {outputDir, protocolFiles};
};

const main = () => {
  const {outputDir, protocolFiles} = parseArgs(process.argv.slice(2));

  const protocolDomains: P.Domain[] = [];
  for (const file of protocolFiles) {
    const protocol = JSON.parse(fs.readFileSync(file, 'utf-8')) as IProtocol;
    protocolDomains.push(...protocol.domains);
  }

  const destProtocolFilePath = path.join(outputDir, 'protocol.d.ts');
  const protocolModuleName = 'protocol';
  emitModule(protocolModuleName, protocolDomains);
  flushEmitToFile(destProtocolFilePath);

  const destMappingFilePath = path.join(outputDir, 'protocol-mapping.d.ts');
  emitMapping('ProtocolMapping', protocolModuleName, protocolDomains);
  flushEmitToFile(destMappingFilePath);

  const destApiFilePath = path.join(outputDir, 'protocol-proxy-api.d.ts');
  emitApi('ProtocolProxyApi', protocolModuleName, protocolDomains);
  flushEmitToFile(destApiFilePath);

  const destTestsApiFilePath =
      path.join(outputDir, 'protocol-tests-proxy-api.d.ts');
  emitApi(
      'ProtocolTestsProxyApi', protocolModuleName, protocolDomains,
      EventStyle.INSPECTOR_PROTOCOL_TESTS);
  flushEmitToFile(destTestsApiFilePath);
};

main();
