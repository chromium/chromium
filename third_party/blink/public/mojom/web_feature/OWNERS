# //PRESUBMIT.py requires the following two lines.
per-file *.mojom=set noparent
per-file *.mojom=file://ipc/SECURITY_OWNERS

# OWNERS review for adding / removing UseCounters comes from the code being
# changed (which is often, but not always in blink/renderer/core).
# No additional review is required for just adding/remove entries from this
# enum (consistent with metrics/histograms/enums.xml).
per-file web_feature.mojom=*
