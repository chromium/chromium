[serialPort_detachedFrame.https.window.html]
  [open() rejects in a detached context]
    expected:
      if product == "chrome": FAIL

  [forget() rejects in a detached context]
    expected:
      if product == "chrome": FAIL
