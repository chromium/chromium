[serial_detachedFrame.https.window.html]
  [getPorts() rejects in a detached context]
    expected:
      if product == "chrome": FAIL

  [requestPort() rejects in a detached context]
    expected:
      if product == "chrome": FAIL

  [adding an event listener does nothing in a detached context]
    expected:
      if product == "chrome": FAIL
