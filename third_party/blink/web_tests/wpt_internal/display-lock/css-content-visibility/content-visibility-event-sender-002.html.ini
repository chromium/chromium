[content-visibility-event-sender-002.html]
  expected:
    if (flag_specific == "") and (os == "linux") and (product == "content_shell"): [OK, CRASH]
  [Trying to focus on an element in a locked subtree that's not visible will skip that element]
    expected:
      if product == "chrome": FAIL
