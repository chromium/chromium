[browser-initiated-popstate.tentative.html]
  expected:
    if product == "chrome": ERROR
  [A soft navigation that uses a *browser initiated* popstate event is recognized by SoftNavigationHeuristics]
    expected:
      if product == "chrome": TIMEOUT
