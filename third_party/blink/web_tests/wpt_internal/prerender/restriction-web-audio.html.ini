[restriction-web-audio.html]
  disabled: times out even with extended deadline
  expected:
    if product == "chrome": TIMEOUT
  [prerendering pages should not be able to start AudioContext]
    expected:
      if product == "chrome": TIMEOUT
