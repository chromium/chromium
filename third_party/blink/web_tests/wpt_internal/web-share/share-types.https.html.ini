[share-types.https.html]
  [share of types other than string (expect implicitly converted to string)]
    expected:
      if product == "chrome": FAIL

  [share of null/undefined dict values]
    expected:
      if product == "chrome": FAIL
