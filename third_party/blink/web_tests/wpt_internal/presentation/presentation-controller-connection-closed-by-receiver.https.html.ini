[presentation-controller-connection-closed-by-receiver.https.html]
  expected:
    if product == "chrome": TIMEOUT
  [receiver connection.close() should fire both controller's and receiver's onclose event handler]
    expected:
      if product == "chrome": TIMEOUT
