[presentation-receiver-terminate-connection.https.html]
  expected:
    if product == "chrome": TIMEOUT
  [receiver connection.terminate() should close receiver frame]
    expected:
      if product == "chrome": TIMEOUT
