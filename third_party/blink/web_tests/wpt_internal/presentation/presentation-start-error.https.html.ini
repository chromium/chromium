[presentation-start-error.https.html]
  expected:
    if product == "chrome": TIMEOUT
  [Test that the PresentationRequest.start() rejects with OperationError if PresentationService reports PREVIOUS_START_IN_PROGRESS error]
    expected:
      if product == "chrome": TIMEOUT
