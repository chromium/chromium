[presentation-controller-terminate-connection.https.html]
  expected:
    if product == "chrome": TIMEOUT
  [Test that controller connection.terminate() fires onterminate event handler.]
    expected:
      if product == "chrome": TIMEOUT
