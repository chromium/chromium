[presentation-reconnect.https.html]
  expected:
    if product == "chrome": TIMEOUT
  [Test that Presentation.reconnect() resolves with existing presentation connection.]
    expected:
      if product == "chrome": TIMEOUT
