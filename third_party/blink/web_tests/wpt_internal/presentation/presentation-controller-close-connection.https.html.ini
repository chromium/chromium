[presentation-controller-close-connection.https.html]
  expected:
    if product == "chrome": TIMEOUT
  [controller connection.close() should fire both controller's and receiver's onclose event handler]
    expected:
      if product == "chrome": TIMEOUT
