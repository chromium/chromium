[abort-signal-memory-tests.any.worker.html]
  [AbortSignalRegistry tracks algorithm handles for event listeners]
    expected:
      if product == "chrome": FAIL


[abort-signal-memory-tests.any.html]
  [AbortSignalRegistry tracks algorithm handles for event listeners]
    expected:
      if product == "chrome": FAIL
