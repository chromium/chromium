[requestmidiaccess-upgrade.https.html]
  expected:
    if product == "chrome": ERROR
  [Test if the second MIDIAccess with stronger authority works correctly]
    expected:
      if product == "chrome": TIMEOUT
