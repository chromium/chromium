[fedcm-reauth-with-signin-status.https.html]
  expected:
    if (product == "chrome") and not debug: [CRASH, TIMEOUT]
  [Test that the returning account from the two accounts will be auto re-authenticated.]
    expected:
      if (product == "chrome") and not debug: TIMEOUT
