[create-selector-directive.html]
  [window.getSelection()]
    expected:
      if (flag_specific == "") and (os == "win"): [PASS, FAIL]

  [Collapsed range.]
    expected:
      if (flag_specific == "") and (os == "win"): [PASS, FAIL]

  [Generate selector within iframe]
    expected:
      if (flag_specific == "") and (os == "win"): [PASS, FAIL]
