[performance-user-timing-special-names.html]
  expected:
    if product == "chrome": ERROR
  [Performance measure processes special names that are defined in User Timing API]
    expected:
      if product == "chrome": FAIL
