CONSOLE MESSAGE: A <ruby> with `display: block` won't render its ruby annotation correctly with future versions of the browser. A workaround is to specify `display: block; display: block ruby;`.
Test propagation of list item marker down to line inside block RUBY.

PASS if no crash or assertion failure.
