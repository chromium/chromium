[mediasource-changetype-play-without-codecs-parameter.html]
  [Test audio-only changeType for audio/mp4; codecs="mp4a.40.2" <-> audio/mp4; codecs="mp4a.40.2" (using types without codecs parameters)]
    expected:
      if (product == "content_shell") and (os == "mac") and (port == "mac12"): FAIL
      if (product == "content_shell") and (os == "win") and (port == "win10.20h2"): FAIL
      if (product == "content_shell") and (os == "linux"): FAIL

  [Test audio-only changeType for audio/mp4; codecs="mp4a.40.2" <-> audio/mpeg (using types without codecs parameters)]
    expected:
      if (product == "content_shell") and (os == "mac") and (port == "mac12"): FAIL
      if (product == "content_shell") and (os == "win") and (port == "win10.20h2"): FAIL
      if (product == "content_shell") and (os == "linux"): FAIL

  [Test audio-only changeType for audio/mp4; codecs="mp4a.40.2" <-> audio/webm; codecs="vorbis" (using types without codecs parameters)]
    expected:
      if (product == "content_shell") and (os == "win") and (port == "win10.20h2"): FAIL
      if (product == "content_shell") and (os == "mac") and (port == "mac12"): FAIL
      if (product == "content_shell") and (os == "linux"): FAIL

  [Test audio-only changeType for audio/mpeg <-> audio/mp4; codecs="mp4a.40.2" (using types without codecs parameters)]
    expected:
      if (product == "content_shell") and (os == "win") and (port == "win10.20h2"): FAIL
      if (product == "content_shell") and (os == "mac") and (port == "mac12"): FAIL
      if (product == "content_shell") and (os == "linux"): FAIL

  [Test audio-only changeType for audio/mpeg <-> audio/webm; codecs="vorbis" (using types without codecs parameters)]
    expected: FAIL

  [Test audio-only changeType for audio/webm; codecs="vorbis" <-> audio/mp4; codecs="mp4a.40.2" (using types without codecs parameters)]
    expected:
      if (product == "content_shell") and (os == "mac") and (port == "mac12"): FAIL
      if (product == "content_shell") and (os == "win") and (port == "win10.20h2"): FAIL
      if (product == "content_shell") and (os == "linux"): FAIL

  [Test audio-only changeType for audio/webm; codecs="vorbis" <-> audio/mpeg (using types without codecs parameters)]
    expected: FAIL

  [Test audio-only changeType for audio/webm; codecs="vorbis" <-> audio/webm; codecs="vorbis" (using types without codecs parameters)]
    expected: FAIL

  [Test video-only changeType for video/mp4; codecs="avc1.4D4001" <-> video/mp4; codecs="avc1.4D4001" (using types without codecs parameters)]
    expected:
      if (product == "content_shell") and (os == "win") and (port == "win10.20h2"): FAIL
      if (product == "content_shell") and (os == "mac") and (port == "mac12"): FAIL
      if (product == "content_shell") and (os == "linux"): FAIL

  [Test video-only changeType for video/mp4; codecs="avc1.4D4001" <-> video/webm; codecs="vp8" (using types without codecs parameters)]
    expected:
      if (product == "content_shell") and (os == "win") and (port == "win10.20h2"): FAIL
      if (product == "content_shell") and (os == "mac") and (port == "mac12"): FAIL
      if (product == "content_shell") and (os == "linux"): FAIL

  [Test video-only changeType for video/mp4; codecs="avc1.4D4001" <-> video/webm; codecs="vp9" (using types without codecs parameters)]
    expected:
      if (product == "content_shell") and (os == "mac") and (port == "mac12"): FAIL
      if (product == "content_shell") and (os == "win") and (port == "win10.20h2"): FAIL
      if (product == "content_shell") and (os == "linux"): FAIL

  [Test video-only changeType for video/webm; codecs="vp8" <-> video/mp4; codecs="avc1.4D4001" (using types without codecs parameters)]
    expected:
      if (product == "content_shell") and (os == "win") and (port == "win10.20h2"): FAIL
      if (product == "content_shell") and (os == "mac") and (port == "mac12"): FAIL
      if (product == "content_shell") and (os == "linux"): FAIL

  [Test video-only changeType for video/webm; codecs="vp8" <-> video/webm; codecs="vp8" (using types without codecs parameters)]
    expected: FAIL

  [Test video-only changeType for video/webm; codecs="vp8" <-> video/webm; codecs="vp9" (using types without codecs parameters)]
    expected: FAIL

  [Test video-only changeType for video/webm; codecs="vp9" <-> video/mp4; codecs="avc1.4D4001" (using types without codecs parameters)]
    expected:
      if (product == "content_shell") and (os == "win") and (port == "win10.20h2"): FAIL
      if (product == "content_shell") and (os == "mac") and (port == "mac12"): FAIL
      if (product == "content_shell") and (os == "linux"): FAIL

  [Test video-only changeType for video/webm; codecs="vp9" <-> video/webm; codecs="vp8" (using types without codecs parameters)]
    expected: FAIL

  [Test video-only changeType for video/webm; codecs="vp9" <-> video/webm; codecs="vp9" (using types without codecs parameters)]
    expected: FAIL
