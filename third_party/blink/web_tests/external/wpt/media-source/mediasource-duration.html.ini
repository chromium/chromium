[mediasource-duration.html]
  expected:
    if (product == "content_shell") and (flag_specific == "") and (os == "linux"): [OK, ERROR]
    if (product == "content_shell") and (flag_specific == "disable-layout-ng"): ERROR
    if (product == "content_shell") and (flag_specific == "disable-site-isolation-trials"): ERROR
  [Test endOfStream completes previous seek to truncated duration]
    expected:
      if (flag_specific == "") and (os == "linux") and (product == "content_shell"): [PASS, FAIL]
