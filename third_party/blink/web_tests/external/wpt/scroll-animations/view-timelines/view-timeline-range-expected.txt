This is a testharness.js-based test.
[FAIL] View timeline with range as strings with relative units.
  assert_throws_js: Invalid font-relative units function "() => {\n      let anim = target.animate(\n        null,\n        {\n          timeline: timeline,\n          rangeStart: "exit 2em",\n          rangeEnd: "exit 8em",\n        },\n      );\n      t.add_cleanup(() => { anim?.cancel(); });\n    }" did not throw
Harness: the test ran to completion.

