[run-url-selection-operation-limit-multiple-origins.tentative.https.sub.html]
  expected:
    if product == "chrome": TIMEOUT
    ERROR
  [selectURL() with overall per-pageload limit]
    expected: TIMEOUT
