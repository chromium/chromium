[multi-screen-fullscreen-enter.tentative.https.html]
  disabled:
    if product == "chrome": times out even with `timeout=long`
  expected:
    if product == "chrome": TIMEOUT
  [Request fullscreen on ']
    expected:
      if product == "chrome": TIMEOUT
