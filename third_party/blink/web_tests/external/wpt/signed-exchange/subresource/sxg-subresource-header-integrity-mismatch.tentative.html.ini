[sxg-subresource-header-integrity-mismatch.tentative.html]
  expected:
    if product == "chrome": TIMEOUT
  [Subresource signed exchange prefetch.]
    expected:
      if product == "content_shell": FAIL
      TIMEOUT
