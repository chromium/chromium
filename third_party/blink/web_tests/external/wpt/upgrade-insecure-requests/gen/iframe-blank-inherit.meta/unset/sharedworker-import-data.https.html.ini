[sharedworker-import-data.https.html]
  expected:
    if product == "chrome": [OK, TIMEOUT]
  [Upgrade-Insecure-Requests: Expects blocked for sharedworker-import-data to same-http-downgrade origin and no-redirect redirection from https context.]
    expected:
      if product == "chrome": [PASS, TIMEOUT]

  [Upgrade-Insecure-Requests: Expects blocked for sharedworker-import-data to same-https origin and downgrade redirection from https context.]
    expected:
      if product == "chrome": [PASS, NOTRUN]
