[worker-fetch.tentative.https.window.html]
  [private to local: failed preflight.]
    expected:
      if (product == "content_shell") and (os == "linux") and (virtual_suite == "pna-iframes-warning") and (flag_specific == "disable-site-isolation-trials"): [PASS, FAIL]
      if (product == "content_shell") and (os == "linux") and (virtual_suite == "keepalive-in-browser-migration") and (flag_specific == ""): [PASS, FAIL]
      if product == "chrome": FAIL

  [private to local: success.]
    expected:
      if product == "chrome": FAIL

  [public to local: failed preflight.]
    expected:
      if (product == "content_shell") and (os == "linux") and (virtual_suite == "pna-iframes-warning") and (flag_specific == "disable-site-isolation-trials"): [PASS, FAIL]
      if (product == "content_shell") and (os == "linux") and (virtual_suite == "keepalive-in-browser-migration") and (flag_specific == ""): [PASS, FAIL]

  [public to private: failed preflight.]
    expected:
      if (product == "content_shell") and (os == "linux") and (virtual_suite == "pna-iframes-warning") and (flag_specific == "disable-site-isolation-trials"): [PASS, FAIL]
      if (product == "content_shell") and (os == "linux") and (virtual_suite == "keepalive-in-browser-migration") and (flag_specific == ""): [PASS, FAIL]

  [treat-as-public to local: failed preflight.]
    expected: FAIL

  [treat-as-public to private: failed preflight.]
    expected: FAIL

  [treat-as-public to private: success.]
    expected:
      if (product == "content_shell") and (os == "linux") and (virtual_suite == "pna-iframes-warning") and (flag_specific == "disable-site-isolation-trials"): [FAIL, PASS]
      if (product == "content_shell") and (os == "linux") and (virtual_suite == "keepalive-in-browser-migration") and (flag_specific == ""): [FAIL, PASS]
      FAIL
