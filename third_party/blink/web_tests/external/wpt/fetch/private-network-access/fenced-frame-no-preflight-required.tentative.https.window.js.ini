[fenced-frame-no-preflight-required.tentative.https.window.html]
  expected:
    if (product == "content_shell") and (virtual_suite == "fenced-frame-mparch") and (os == "linux") and (flag_specific == ""): [OK, TIMEOUT]
  [local to local: no preflight required.]
    expected:
      if (product == "content_shell") and (virtual_suite == "fenced-frame-mparch") and (os == "linux") and (flag_specific == ""): PASS
      FAIL

  [local to private: no preflight required.]
    expected:
      if (product == "content_shell") and (virtual_suite == "fenced-frame-mparch") and (os == "linux") and (flag_specific == ""): PASS
      FAIL

  [local to public: no preflight required.]
    expected:
      if (product == "content_shell") and (virtual_suite == "fenced-frame-mparch") and (os == "linux") and (flag_specific == ""): PASS
      FAIL

  [private to private: no preflight required.]
    expected:
      if (product == "content_shell") and (virtual_suite == "fenced-frame-mparch") and (os == "linux") and (flag_specific == ""): PASS
      FAIL

  [private to public: no preflight required.]
    expected:
      if (product == "content_shell") and (virtual_suite == "fenced-frame-mparch") and (os == "linux") and (flag_specific == ""): PASS
      FAIL

  [public to public: no preflight required.]
    expected:
      if (product == "content_shell") and (virtual_suite == "fenced-frame-mparch") and (os == "linux") and (flag_specific == ""): PASS
      FAIL

  [treat-as-public-address to local: optional preflight]
    expected:
      if (product == "content_shell") and (virtual_suite == "fenced-frame-mparch") and (os == "linux") and (flag_specific == ""): PASS
      FAIL

  [treat-as-public-address to public: no preflight required.]
    expected:
      if (product == "content_shell") and (virtual_suite == "fenced-frame-mparch") and (os == "linux") and (flag_specific == ""): PASS
      FAIL
