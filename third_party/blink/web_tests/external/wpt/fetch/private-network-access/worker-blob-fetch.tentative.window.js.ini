[worker-blob-fetch.tentative.window.html]
  expected:
    if (product == "content_shell") and (virtual_suite == "pna-permission") and (os == "linux") and (flag_specific == "disable-site-isolation-trials"): [OK, TIMEOUT]
    if (product == "content_shell") and (virtual_suite == "") and (os == "linux") and (flag_specific == "disable-site-isolation-trials"): [OK, TIMEOUT]
  [private https to local https: failure.]
    expected:
      if product == "chrome": FAIL

  [private to local: failure.]
    expected:
      if product == "chrome": FAIL
