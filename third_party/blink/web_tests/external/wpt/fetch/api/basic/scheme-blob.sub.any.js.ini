[scheme-blob.sub.any.html]
  [Fetching URL.createObjectURL(invalid_type_blob) is OK]
    expected:
      if not debug and (product == "content_shell") and (os == "mac") and (port == "mac10.15"): PASS
      if debug: PASS
      [FAIL, PASS]


[scheme-blob.sub.any.worker.html]
  [Fetching URL.createObjectURL(invalid_type_blob) is OK]
    expected:
      if not debug and (product == "content_shell") and (os == "mac") and (port == "mac10.15"): PASS
      if debug: PASS
      [FAIL, PASS]
