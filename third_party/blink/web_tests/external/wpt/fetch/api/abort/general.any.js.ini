[general.any.html]
  [Aborting rejects with abort reason]
    expected:
      if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): PASS
      FAIL

  [Clone aborts with original controller]
    expected:
      if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): FAIL

  [Readable stream synchronously cancels with AbortError if aborted before reading]
    expected: FAIL

  [Signal on request object should also have abort reason]
    expected:
      if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): PASS
      FAIL


[general.any.serviceworker.html]
  [Aborting rejects with abort reason]
    expected:
      if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): [PASS, FAIL]
      FAIL

  [Clone aborts with original controller]
    expected:
      if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): [FAIL, PASS]

  [Readable stream synchronously cancels with AbortError if aborted before reading]
    expected: FAIL

  [Signal on request object should also have abort reason]
    expected:
      if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): [PASS, FAIL]
      FAIL

  [Stream errors once aborted. Underlying connection closed.]
    expected:
      if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): [PASS, FAIL]


[general.any.sharedworker.html]
  expected:
    if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): [OK, CRASH]
  [Aborting rejects with abort reason]
    expected:
      if (product == "content_shell") and (virtual_suite == "produce-compile-hints"): [FAIL, PASS]
      if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): [PASS, FAIL]
      FAIL

  [Clone aborts with original controller]
    expected:
      if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): [FAIL, PASS]

  [Readable stream synchronously cancels with AbortError if aborted before reading]
    expected: FAIL

  [Signal on request object should also have abort reason]
    expected:
      if (product == "content_shell") and (virtual_suite == "produce-compile-hints"): [FAIL, PASS]
      if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): [PASS, FAIL]
      FAIL


[general.any.worker.html]
  [Aborting rejects with abort reason]
    expected:
      if (product == "content_shell") and (virtual_suite == "keepalive-in-browser-migration") and (os == "linux") and (flag_specific == ""): [PASS, FAIL]
      if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): [PASS, FAIL]
      FAIL

  [Clone aborts with original controller]
    expected:
      if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): [PASS, FAIL]

  [Readable stream synchronously cancels with AbortError if aborted before reading]
    expected: FAIL

  [Signal on request object should also have abort reason]
    expected:
      if (product == "content_shell") and (virtual_suite == "keepalive-in-browser-migration") and (os == "linux") and (flag_specific == ""): [PASS, FAIL]
      if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): [PASS, FAIL]
      FAIL

  [Stream errors once aborted, after reading. Underlying connection closed.]
    expected:
      if (product == "content_shell") and (virtual_suite == "abort-signal-any-disabled"): [PASS, FAIL]
