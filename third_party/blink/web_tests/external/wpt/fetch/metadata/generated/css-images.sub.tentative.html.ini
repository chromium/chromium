[css-images.sub.tentative.html]
  disabled:
    if product == "chrome": times out even with `timeout=long`
    if (product == "content_shell") and (virtual_suite == "") and (os == "linux") and (flag_specific == ""): times out even with `timeout=long`
    if (product == "content_shell") and (virtual_suite == "") and (os == "linux") and (flag_specific == "disable-site-isolation-trials"): times out even with `timeout=long`
    if (product == "content_shell") and (virtual_suite == "") and (os == "mac"): times out even with `timeout=long`
    if (product == "content_shell") and (virtual_suite == "") and (os == "win"): times out even with `timeout=long`
    if (product == "content_shell") and (virtual_suite == "keepalive-in-browser-migration") and (os == "linux") and (flag_specific == ""): times out even with `timeout=long`
    if (product == "content_shell") and (virtual_suite == "keepalive-in-browser-migration") and (os == "linux") and (flag_specific == "disable-site-isolation-trials"): times out even with `timeout=long`
    if (product == "content_shell") and (virtual_suite == "keepalive-in-browser-migration") and (os == "mac"): times out even with `timeout=long`
    if (product == "content_shell") and (virtual_suite == "keepalive-in-browser-migration") and (os == "win"): times out even with `timeout=long`
    if (product == "content_shell") and (virtual_suite == "plz-dedicated-worker") and (os == "linux") and (flag_specific == ""): times out even with `timeout=long`
    if (product == "content_shell") and (virtual_suite == "plz-dedicated-worker") and (os == "linux") and (flag_specific == "disable-site-isolation-trials"): times out even with `timeout=long`
    if (product == "content_shell") and (virtual_suite == "produce-compile-hints") and (os == "linux") and (flag_specific == ""): times out even with `timeout=long`
    if (product == "content_shell") and (virtual_suite == "produce-compile-hints") and (os == "linux") and (flag_specific == "disable-site-isolation-trials"): times out even with `timeout=long`
  expected: TIMEOUT
  [background-image sec-fetch-site - HTTPS downgrade-upgrade]
    expected: TIMEOUT

  [background-image sec-fetch-site - HTTPS upgrade]
    expected: TIMEOUT
