[pointerevent_touch-action-modified_touch.html]
  expected:
    if (product == "chrome") and debug: [OK, ERROR]
    if (product == "chrome") and not debug: [CRASH, OK, ERROR]
    ERROR
  [Deleting touch-action elem after pointerdown]
    expected:
      if product == "chrome": FAIL
