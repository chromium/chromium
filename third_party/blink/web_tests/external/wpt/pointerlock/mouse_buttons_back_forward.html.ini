[mouse_buttons_back_forward.html]
  expected:
    if (product == "content_shell") and (os == "mac") and (port == "mac13"): [TIMEOUT, CRASH]
    if (product == "content_shell") and (os == "win"): OK
    if (product == "content_shell") and (os == "linux"): OK
    if product == "chrome": OK
    TIMEOUT
