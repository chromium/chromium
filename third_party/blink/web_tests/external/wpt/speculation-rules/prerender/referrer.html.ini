[referrer.html]
  expected:
    if (product == "content_shell") and (os == "win"): [OK, TIMEOUT]
    if product == "chrome": TIMEOUT
  [default referrer]
    expected:
      if product == "chrome": TIMEOUT
