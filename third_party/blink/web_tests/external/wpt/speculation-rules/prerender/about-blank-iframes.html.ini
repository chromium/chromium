[about-blank-iframes.html]
  disabled:
    if product == "chrome": times out even with `timeout=long`
  expected:
    if product == "chrome": TIMEOUT
  [about:blank iframes]
    expected:
      if product == "chrome": TIMEOUT
