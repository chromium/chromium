[referrer-policy-no-referrer.html]
  expected:
    if (os == "linux") and (product == "chrome"): TIMEOUT
    if os == "win": TIMEOUT
  [no referrer]
    expected:
      if product == "chrome": TIMEOUT
