[referrer-policy-origin.html]
  expected:
    if (os == "linux") and (product == "chrome"): TIMEOUT
    if os == "win": TIMEOUT
  [origin referrer]
    expected:
      if product == "chrome": TIMEOUT
