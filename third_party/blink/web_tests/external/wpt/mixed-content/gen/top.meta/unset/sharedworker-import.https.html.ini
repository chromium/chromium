[sharedworker-import.https.html]
  expected:
    if product == "chrome": TIMEOUT
  [Mixed-Content: Expects blocked for sharedworker-import to same-http origin and keep-scheme redirection from https context.]
    expected:
      if product == "chrome": NOTRUN

  [Mixed-Content: Expects blocked for sharedworker-import to same-http origin and swap-scheme redirection from https context.]
    expected:
      if product == "chrome": NOTRUN

  [Mixed-Content: Expects blocked for sharedworker-import to cross-http origin and swap-scheme redirection from https context.]
    expected:
      if product == "chrome": TIMEOUT

  [Mixed-Content: Expects blocked for sharedworker-import to same-http origin and no-redirect redirection from https context.]
    expected:
      if product == "chrome": NOTRUN
