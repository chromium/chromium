[finished.html]
  [Test finished promise changes for animation duration changes]
    expected:
      if (os == "linux") and (flag_specific == "") and (product == "chrome"): [PASS, FAIL]
      if (os == "linux") and (flag_specific == "disable-layout-ng"): [PASS, FAIL]
