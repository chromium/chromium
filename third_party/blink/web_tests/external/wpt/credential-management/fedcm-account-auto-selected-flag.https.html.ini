[fedcm-account-auto-selected-flag.https.html]
  expected:
    if product == "chrome": [CRASH, ERROR, TIMEOUT]
  [Test that the is_account_auto_selected bit is properly sent.]
    expected:
      if product == "chrome": TIMEOUT
