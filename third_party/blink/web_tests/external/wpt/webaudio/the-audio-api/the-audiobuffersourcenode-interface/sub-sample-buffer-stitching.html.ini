[sub-sample-buffer-stitching.html]
  expected:
    if product == "chrome": [OK, TIMEOUT]
  [Executing "buffer-stitching-2"]
    expected:
      if product == "chrome": [PASS, TIMEOUT]

  [Audit report]
    expected:
      if product == "chrome": [PASS, NOTRUN]
