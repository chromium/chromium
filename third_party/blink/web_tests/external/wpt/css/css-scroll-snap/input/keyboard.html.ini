[keyboard.html]
  expected:
    if (product == "content_shell") and (os == "win") and (virtual_suite == "") and (port == "win10.20h2"): [OK, TIMEOUT]
    if (product == "content_shell") and (os == "mac") and (port == "mac13"): TIMEOUT
    if (product == "content_shell") and (os == "mac") and (port == "mac12"): [OK, TIMEOUT]
  [If the original intended offset is valid as making a snap area cover the snapport, but there's a defined snap offset in between, use the defined snap offset.]
    expected:
      if (product == "content_shell") and (os == "win") and (virtual_suite == "threaded") and (port == "win10.20h2"): [PASS, FAIL]

  [If the original intended offset is valid as making a snap area cover thesnapport, and there's no other snap offset in between, use the originalintended offset]
    expected:
      if (product == "content_shell") and (os == "win") and (virtual_suite == "threaded") and (port == "win10.20h2"): [PASS, FAIL]

  [If there is no valid snap offset on the arrow key's direction other than the current offset, and the scroll-snap-type is proximity, go to the original intended offset]
    expected:
      if (product == "content_shell") and (os == "win") and (virtual_suite == "threaded") and (port == "win10.20h2"): [PASS, FAIL]

  [Snaps to bottom-left after pressing ArrowDown]
    expected:
      if (product == "content_shell") and (os == "win") and (virtual_suite == "threaded") and (port == "win11"): PASS
      if (product == "content_shell") and (os == "win") and (virtual_suite == "threaded") and (port == "win10.20h2"): [PASS, FAIL]
      if (product == "content_shell") and (os == "win") and (virtual_suite == ""): PASS
      if (product == "content_shell") and (os == "mac") and (port == "mac12"): [FAIL, PASS]
      if (product == "content_shell") and (os == "linux"): PASS
      if product == "chrome": PASS
      FAIL

  [Snaps to top-left after pressing ArrowLeft]
    expected:
      if (product == "content_shell") and (os == "win") and (virtual_suite == "threaded") and (port == "win10.20h2"): [PASS, FAIL]

  [Snaps to top-left after pressing ArrowUp]
    expected:
      if (product == "content_shell") and (os == "win") and (virtual_suite == "threaded") and (port == "win10.20h2"): [FAIL, PASS]

  [Snaps to top-right after pressing ArrowRight]
    expected:
      if (product == "content_shell") and (os == "win") and (virtual_suite == "threaded") and (port == "win10.20h2"): [FAIL, PASS]
