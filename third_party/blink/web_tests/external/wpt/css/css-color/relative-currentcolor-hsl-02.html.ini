[relative-currentcolor-hsl-02.html]
  expected:
    if (product == "content_shell") and (os == "linux") and (flag_specific == "disable-site-isolation-trials"): [CRASH, TIMEOUT]
    if (product == "content_shell") and (os == "linux") and (flag_specific == ""): [CRASH, TIMEOUT]
    CRASH
