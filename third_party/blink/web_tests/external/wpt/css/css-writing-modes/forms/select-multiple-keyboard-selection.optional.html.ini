[select-multiple-keyboard-selection.optional.html]
  [select[multiple\][style="writing-mode: horizontal-tb"\] supports keyboard navigation]
    expected:
      if (product == "content_shell") and (os == "mac") and (port == "mac10.15"): PASS
      if product == "chrome": PASS
      FAIL

  [select[multiple\][style="writing-mode: vertical-lr"\] supports keyboard navigation]
    expected:
      if (product == "content_shell") and (os == "mac") and (port == "mac10.15"): PASS
      if product == "chrome": PASS
      FAIL

  [select[multiple\][style="writing-mode: vertical-rl"\] supports keyboard navigation]
    expected:
      if (product == "content_shell") and (os == "mac") and (port == "mac10.15"): PASS
      if product == "chrome": PASS
      FAIL
