[parse-input-arguments-018.https.html]
  expected:
    if (flag_specific == "") and (product == "content_shell"): [CRASH, PASS]
    if (flag_specific == "") and (product == "chrome"): [PASS, CRASH]
    CRASH
