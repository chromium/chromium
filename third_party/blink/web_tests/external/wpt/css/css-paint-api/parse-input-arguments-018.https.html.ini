[parse-input-arguments-018.https.html]
  expected:
    if (product == "content_shell") and (os == "win") and (virtual_suite == "off-main-thread-css-paint") and (port == "win11"): [CRASH, PASS, TIMEOUT]
    if (product == "content_shell") and (os == "win") and (virtual_suite == "") and (port == "win10.20h2"): [CRASH, PASS]
    if (product == "content_shell") and (os == "mac") and (port == "mac12") and (virtual_suite == ""): [CRASH, PASS]
    if (product == "content_shell") and (os == "mac") and (port == "mac11") and (virtual_suite == ""): [CRASH, PASS]
    if (product == "content_shell") and (os == "linux") and (flag_specific == "disable-site-isolation-trials") and (virtual_suite == "off-main-thread-css-paint"): [TIMEOUT, PASS, CRASH]
    if (product == "content_shell") and (os == "linux") and (flag_specific == ""): [TIMEOUT, CRASH]
    if product == "chrome": [CRASH, PASS]
    CRASH
