[parse-input-arguments-018.https.html]
  expected:
    if (product == "content_shell") and (flag_specific == "") and (os == "linux"): [PASS, CRASH]
    if (product == "content_shell") and (flag_specific == "") and (os == "win"): [PASS, CRASH]
    CRASH
