[disabled.html]
  [Test that a soft navigation is not detected when the feature is disabled]
    expected:
      if (product == "content_shell") and (virtual_suite == "") and (os == "mac") and (port == "mac11-arm64"): PASS
      if (product == "content_shell") and (virtual_suite == "stable"): PASS
      FAIL
