// DO NOT EDIT! This test has been generated by /html/canvas/tools/gentest.py.
// OffscreenCanvas test in a worker:2d.state.saverestore.miterLimit
// Description:
// Note:

importScripts("/resources/testharness.js");
importScripts("/html/canvas/resources/canvas-tests.js");

test(t => {
  const canvas = new OffscreenCanvas(300, 150);
  const ctx = canvas.getContext('2d');

  // Test that restore() undoes any modifications.
  var old = ctx.miterLimit;
  ctx.save();
  ctx.miterLimit = 0.5;
  ctx.restore();
  _assertSame(ctx.miterLimit, old, "ctx.miterLimit", "old");

  // Also test that save() doesn't modify the values.
  ctx.miterLimit = 0.5;
  old = ctx.miterLimit;
  // We're not interested in failures caused by get(set(x)) != x (e.g.
  // from rounding), so compare against `old` instead of against 0.5.
  ctx.save();
  _assertSame(ctx.miterLimit, old, "ctx.miterLimit", "old");
  ctx.restore();
}, "save()/restore() works for miterLimit, with a canvas size of (300, 150)");

test(t => {
  const canvas = new OffscreenCanvas(0, 0);
  const ctx = canvas.getContext('2d');

  // Test that restore() undoes any modifications.
  var old = ctx.miterLimit;
  ctx.save();
  ctx.miterLimit = 0.5;
  ctx.restore();
  _assertSame(ctx.miterLimit, old, "ctx.miterLimit", "old");

  // Also test that save() doesn't modify the values.
  ctx.miterLimit = 0.5;
  old = ctx.miterLimit;
  // We're not interested in failures caused by get(set(x)) != x (e.g.
  // from rounding), so compare against `old` instead of against 0.5.
  ctx.save();
  _assertSame(ctx.miterLimit, old, "ctx.miterLimit", "old");
  ctx.restore();
}, "save()/restore() works for miterLimit, with a canvas size of (0, 0)");

done();
