[property-window.https.html]
  expected: [ERROR, OK]
  [cross-origin > w => w.window]
    expected:
      if product == "chrome": FAIL

  [same-origin > w => w.window]
    expected:
      if product == "chrome": FAIL
