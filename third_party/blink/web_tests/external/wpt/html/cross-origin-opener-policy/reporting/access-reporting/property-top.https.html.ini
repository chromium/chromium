[property-top.https.html]
  expected:
    if product == "chrome": [ERROR, OK]
  [same-origin > w => w.top]
    expected:
      if product == "chrome": FAIL

  [cross-origin > w => w.top]
    expected:
      if product == "chrome": FAIL
