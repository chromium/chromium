[selectmenu-form-state-restore.tentative.html]
  expected:
    if (product == "content_shell") and (flag_specific == "") and (os == "linux"): [OK, ERROR, TIMEOUT]
    if product == "chrome": OK
    ERROR
