[sandbox-top-navigation-child-special-cases.tentative.sub.window.html]
  expected:
    if (product == "content_shell") and (flag_specific == "disable-layout-ng"): OK
    if product == "chrome": ERROR
    [OK, TIMEOUT]
  [Allow top with user activation + user activation]
    expected:
      if flag_specific == "disable-site-isolation-trials": [PASS, FAIL]
