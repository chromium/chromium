[focus.html]
  [Focus should be kept when page gets into and out of BFCache]
    expected:
      if (product == "content_shell") and (os == "win"): [FAIL, PRECONDITION_FAILED]
      if product == "chrome": PRECONDITION_FAILED
      FAIL
