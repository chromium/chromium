[reporting-subresource-corp.https.window.html]
  expected:
    if (flag_specific == "") and (product == "chrome"): [OK, TIMEOUT]
  [reporting-subresource-corp]
    expected:
      if product == "chrome": [PASS, TIMEOUT]
