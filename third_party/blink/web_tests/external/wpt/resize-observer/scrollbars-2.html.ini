[scrollbars-2.html]
  expected:
    if (product == "content_shell") and (flag_specific == "highdpi"): [OK, CRASH]
    if product == "chrome": OK
    CRASH
  [ResizeObserver content-box size and scrollbars]
    expected: FAIL
