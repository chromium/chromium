[partitioned-web-locks.tentative.https.html]
  [WebLocks of a nested iframe with a cross-site ancestor are partitioned]
    expected:
      if (product == "content_shell") and (os == "win") and (virtual_suite == "third-party-storage-partitioning") and (port == "win11"): [FAIL, PASS]
      if (product == "content_shell") and (os == "mac") and (port == "mac13") and (virtual_suite == "third-party-storage-partitioning"): FAIL

  [WebLocks of an iframe under a 3rd-party site are partitioned]
    expected:
      if (product == "content_shell") and (os == "win") and (virtual_suite == "third-party-storage-partitioning") and (port == "win11"): [FAIL, PASS]
      if (product == "content_shell") and (os == "mac") and (port == "mac13") and (virtual_suite == "third-party-storage-partitioning"): FAIL
