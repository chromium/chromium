[detach_iframe_during_open.https.window.html]
  expected:
    if (os == "linux") and (flag_specific == "") and (product == "content_shell"): [CRASH, OK]
    if (os == "linux") and (flag_specific == "disable-site-isolation-trials"): [CRASH, OK]
    if os == "win": [OK, TIMEOUT, CRASH]
