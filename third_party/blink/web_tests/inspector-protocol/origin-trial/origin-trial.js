(async function(/** @type {import('test_runner').TestRunner} */ testRunner) {
  // Token generated by running
  // tools/origin_trials/generate_token.py "http://127.0.0.1:8000" \
  // Frobulate --expire-timestamp=1761176198
  const {dp} = await testRunner.startHTML(`
  <meta http-equiv="origin-trial"
        content="Azyb0NvqGpCAlOx91FP6wa6pn5iu6+UTVuKv+H3QHoFQeKuMqqgJdwbpON0LpCSSpk6VZcR3/DULpkLjrQbViQUAAABReyJvcmlnaW4iOiAiaHR0cDovLzEyNy4wLjAuMTo4MDAwIiwgImZlYXR1cmUiOiAiRnJvYnVsYXRlIiwgImV4cGlyeSI6IDE3NjExNzYxOTh9"
  />
  `, 'Verifies that we can successfully retrieve origin trial in frame tree.');

  await dp.Page.enable();
  const frameId = (await dp.Page.getResourceTree()).result.frameTree.frame.id;
  const result = (await dp.Page.getOriginTrials({frameId})).result;
  testRunner.log(result.originTrials);
  testRunner.completeTest();
});
