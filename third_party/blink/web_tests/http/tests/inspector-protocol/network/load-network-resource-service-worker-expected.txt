Tests that we can load resources from a service worker.
Response for fetch with existing resource: {
    resource : {
        headers : <object>
        httpStatusCode : 200
        stream : <string>
        success : true
    }
}

