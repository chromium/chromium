Tests with the CSSLineClampLinesAndHeight feature.

This feature makes `(-webkit-)line-clamp: <integer>` specified
together with `(max-)height` clamp by both lines and height, even
though this behavior was determined not to be web compatible for this
syntax.
