# This suite contains resize observer tests run
# with the flags --force-device-scale-factor=2 and --enable-use-zoom-for-dsf=false
# TODO(savella): Remove this virtual test when all platforms are migrated to use zoom for dsf.
