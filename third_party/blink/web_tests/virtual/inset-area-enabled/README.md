# This suite runs tests with --enable-blink-features=CSSInsetAreaProperty and --disable-blink-features=CSSPositionAreaProperty.
# It verifies that the old `inset-area` property still works correctly in this case.

