This directory is for testing event timing with the `EventTimingReportingInStrictOrderOnly` kill switch disabled.
This ensures that event timing entries are still reported even if an interactionId is not yet assigned.
