The content-extraction virtual re-runs a handful of web tests to exercise a
helping to ensure that DCHECKs and crashes are not triggered by a variety
of web content.

The APC ContentNode tree is automatically built on load.
