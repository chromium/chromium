# Back-to-ad Intervention DevTools Issues

This virtual test suite verifies that DevTools issues are correctly reported
when the back-to-ad intervention would trigger.

The tests are run with the flag --enable-features=BackToAdIntervention
