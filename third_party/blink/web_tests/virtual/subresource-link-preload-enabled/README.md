# Overview

This suite runs a subset of tests with `--enable-features=RestrictLinkHeaderOnSubresource:disable_resource_load/false`, to make
sure no behavior is broken when the subresource link preload restriction is disabled.
