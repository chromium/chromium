This suite runs tests with --enable-features=SystemColorChooser

Currently this feature is only being used by macOS. You can find the
macOS baselines here:

../../platform/mac*/virtual/system-color-picker-appearance/
