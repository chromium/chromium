This directory is for testing event timing with the `NavigationEventTiming` feature flag enabled.
This feature flag opt-in to reporting some extensions to the Event Timing API,
which are otherwise measured but not reported.  Useful alongside Soft Navs
measurement, but not required.
