# virtual/partitioned-cookies

This directory is for tests that need the Partitioned Cookies feature enabled.
Tests under `virtual/partitioned-cookies` are run with `--enable-features=PartitionedCookies`.
