# This suite runs tests with --disable-features=StreamingDeclarativeShadowDOM.

The non-streaming-tests folder contains a copy of the older tests, which examine the behavior of the 'shadowroot' attribute.
