Tests with DataUrlMimeTypeParameterPreservation feature enabled.

This feature preserves MIME type parameters (e.g., boundary=xxx) in data: URL
Content-Type headers per the Fetch Standard.
See https://fetch.spec.whatwg.org/#data-url-processor
