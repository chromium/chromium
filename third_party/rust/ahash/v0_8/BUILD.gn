# Copyright 2023 The Chromium Authors
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

# @generated from third_party/rust/chromium_crates_io/BUILD.gn.hbs by
# tools/crates/gnrt.
# Do not edit!

import("//build/rust/cargo_crate.gni")

cargo_crate("lib") {
  crate_name = "ahash"
  epoch = "0.8"
  crate_type = "rlib"
  crate_root =
      "//third_party/rust/chromium_crates_io/vendor/ahash-v0_8/src/lib.rs"
  sources = [
    "//third_party/rust/chromium_crates_io/vendor/ahash-v0_8/src/aes_hash.rs",
    "//third_party/rust/chromium_crates_io/vendor/ahash-v0_8/src/convert.rs",
    "//third_party/rust/chromium_crates_io/vendor/ahash-v0_8/src/fallback_hash.rs",
    "//third_party/rust/chromium_crates_io/vendor/ahash-v0_8/src/hash_map.rs",
    "//third_party/rust/chromium_crates_io/vendor/ahash-v0_8/src/hash_quality_test.rs",
    "//third_party/rust/chromium_crates_io/vendor/ahash-v0_8/src/hash_set.rs",
    "//third_party/rust/chromium_crates_io/vendor/ahash-v0_8/src/lib.rs",
    "//third_party/rust/chromium_crates_io/vendor/ahash-v0_8/src/operations.rs",
    "//third_party/rust/chromium_crates_io/vendor/ahash-v0_8/src/random_state.rs",
    "//third_party/rust/chromium_crates_io/vendor/ahash-v0_8/src/specialize.rs",
  ]
  inputs = []

  build_native_rust_unit_tests = false
  edition = "2018"
  cargo_pkg_authors = "Tom Kaitchuck <Tom.Kaitchuck@gmail.com>"
  cargo_pkg_name = "ahash"
  cargo_pkg_description =
      "A non-cryptographic hash function using AES-NI for high performance"
  cargo_pkg_repository = "https://github.com/tkaitchuck/ahash"
  cargo_pkg_version = "0.8.12"

  allow_unsafe = true

  deps = [
    "//third_party/rust/cfg_if/v1:lib",
    "//third_party/rust/once_cell/v1:lib",
    "//third_party/rust/zerocopy/v0_8:lib",
  ]
  build_deps = [ "//third_party/rust/version_check/v0_9:buildrs_support" ]
  build_root =
      "//third_party/rust/chromium_crates_io/vendor/ahash-v0_8/build.rs"
  build_sources =
      [ "//third_party/rust/chromium_crates_io/vendor/ahash-v0_8/build.rs" ]

  # Only for usage from third-party crates. Add the crate to
  # //third_party/rust/chromium_crates_io/Cargo.toml to use
  # it from first-party code.
  visibility = [ "//third_party/rust/*" ]
  testonly = true

  #####################################################################
  # Tweaking which GN `config`s apply to this target.

  # Config changes that apply to all `//third_party/rust` crates.
  _configs_to_remove = [
    # We don't need code coverage data for any `chromium_crates_io` crates.
    "//build/config/coverage:default_coverage",

    # This is third-party code, so remove `chromium_code` config.  We are not
    # at the same time adding `//build/config/compiler:no_chromium_code`,
    # because 1) we don't want to pull how warnings are handled by that config
    # and 2) that config doesn't have any non-warnings-related stuff.
    "//build/config/compiler:chromium_code",
  ]
  _configs_to_add = []

  # Changing (if needed) which configs apply to this specific crate (based on
  # `extra_kv.configs_to_remove` and `extra_kv.configs_to_add` from
  # `gnrt_config.toml`).
  _configs_to_remove += []
  _configs_to_add += []

  # Applying config changes.
  library_configs -= _configs_to_remove
  library_configs += _configs_to_add
  executable_configs -= _configs_to_remove
  executable_configs += _configs_to_add
  proc_macro_configs -= _configs_to_remove
  proc_macro_configs += _configs_to_add
}
